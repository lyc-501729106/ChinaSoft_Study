package class0814.home;

/**
 * trim() ; 此方法可以去除字符串的前面和后面的所有空格,但是字符串内部的空格不会去掉；
 *
 */
public class TrimTest {
    public static void main(String[] args) {
        String str = " he llo " ;
        System.out.println(str.trim());
        System.out.println(str);

    }
}
